# 📋 Документация по CMD скриптам

Этот документ описывает использование cmd-скриптов для проверки и запуска системы Cloud.ru AI.

## 📁 Доступные скрипты

### 1. `setup_env.cmd` - Установка окружения (ОБЯЗАТЕЛЬНО ПЕРВЫМ!)
**Назначение:** Автоматическая установка виртуального окружения и всех зависимостей.

**Что делает:**
- ✅ Проверяет наличие Python
- ✅ Создает виртуальное окружение в `qik-test-qa-server\ai\.venv`
- ✅ Активирует окружение
- ✅ Обновляет pip до последней версии
- ✅ Устанавливает все зависимости из `requirements.txt`
- ✅ Проверяет установку ключевых пакетов (nats-py, openai, fastapi)

**Использование:**
```cmd
setup_env.cmd
```

**Важно:**
- **ЗАПУСТИТЕ ЭТОТ СКРИПТ ПЕРВЫМ** перед использованием других скриптов
- Скрипт должен запускаться из корня проекта `qik-sigma-test`
- Если виртуальное окружение уже существует, скрипт предложит пересоздать его

---

### 2. `check_system.cmd` - Полная проверка системы
**Назначение:** Комплексная проверка всех зависимостей, настроек и компонентов системы.

**Что проверяет:**
- ✅ Docker и Docker Desktop
- ✅ Python (версия 3.8+)
- ✅ Виртуальное окружение и установленные пакеты
- ✅ Структура проекта
- ✅ Docker Compose
- ✅ NATS контейнер и порт 4222
- ✅ Переменные окружения

**Использование:**
```cmd
check_system.cmd
```

**Выходные коды:**
- `0` - Система готова (могут быть предупреждения)
- `1` - Обнаружены критические ошибки

---

### 2. `quick_check.cmd` - Быстрая проверка
**Назначение:** Быстрая проверка основных компонентов без детального анализа.

**Что проверяет:**
- Docker работает
- Python найден
- NATS запущен
- Виртуальное окружение найдено

**Использование:**
```cmd
quick_check.cmd
```

---

### 3. `start_server.cmd` - Запуск сервера
**Назначение:** Запуск NATS и AI Worker.

**Что делает:**
1. Проверяет зависимости (Docker, Python)
2. Настраивает переменные окружения
3. Проверяет/активирует виртуальное окружение
4. Запускает NATS через Docker Compose
5. Проверяет доступность NATS
6. Запускает AI Worker

**Использование:**
```cmd
start_server.cmd
```

**Важно:**
- Убедитесь, что Docker Desktop запущен
- Перед первым запуском установите переменные окружения или измените их в скрипте
- Скрипт должен запускаться из корня проекта `qik-sigma-test`

**Переменные окружения (можно установить заранее):**
```cmd
set NATS_URL=nats://localhost:4222
set USE_REAL_LLM=true
set LLM_API_KEY=your_api_key_here
set LLM_BASE_URL=https://foundation-models.api.cloud.ru/v1/chat/completions
set LLM_MODEL=Qwen/Qwen3-Coder-480B-A35B-Instruct
set GITHUB_PAT=your_github_token_here
```

---

### 4. `run_task.cmd` - Запуск задач генерации
**Назначение:** Запуск различных задач генерации тестов через NATS.

**Доступные режимы:**
- `ui` - Генерация ручных UI тестов (15+ кейсов в Markdown)
- `api` - Генерация ручных API тестов (15+ кейсов в Markdown)
- `e2e` - Генерация E2E Playwright тестов (Python + pytest)
- `api_tests` - Генерация API тестов (Python + pytest, httpx)
- `cicd` - CI/CD Push (требует run_id от предыдущей генерации)
- `gen` - Простая генерация тестов

**Использование:**
```cmd
run_task.cmd
```
Затем выберите режим из предложенного списка.

**Требования:**
- NATS должен быть запущен (используйте `start_server.cmd`)
- AI Worker должен быть запущен (запускается через `start_server.cmd`)
- Для режима `cicd` требуется `run_id` от предыдущей генерации

---

## 🔄 Типичный workflow

### Первый запуск:
1. **Установка окружения (ОБЯЗАТЕЛЬНО!):**
   ```cmd
   setup_env.cmd
   ```
   Это установит виртуальное окружение и все зависимости.

2. **Проверка системы (рекомендуется):**
   ```cmd
   check_system.cmd
   ```

3. **Настройка переменных окружения:**
   - Установите переменные окружения или измените их в `start_server.cmd`

4. **Запуск сервера:**
   ```cmd
   start_server.cmd
   ```
   (Оставьте это окно открытым - здесь работает Worker)

5. **В новом окне - запуск задачи:**
   ```cmd
   run_task.cmd
   ```

### Последующие запуски:
1. **Быстрая проверка:**
   ```cmd
   quick_check.cmd
   ```

2. **Запуск сервера:**
   ```cmd
   start_server.cmd
   ```

3. **Запуск задач:**
   ```cmd
   run_task.cmd
   ```

---

## ⚠️ Важные замечания

### Пути к файлам
Все скрипты должны запускаться из корня проекта `qik-sigma-test`. Структура должна быть:
```
qik-sigma-test/
├── check_system.cmd
├── quick_check.cmd
├── start_server.cmd
├── run_task.cmd
└── qik-test-qa-server/
    ├── ai/
    │   ├── apps/
    │   ├── libs/
    │   └── nats_publish_*.py
    └── deploy/
        └── compose.proxy.yml
```

### NATS URL
Python скрипты используют хардкод адресов NATS:
- `nats://nats:4222` - для Docker compose (внутри контейнера)
- `nats://127.0.0.1:4222` - для локального запуска (в `nats_publish_cicd_push.py`)

Для локального запуска убедитесь, что NATS доступен по адресу `nats://localhost:4222` или `nats://127.0.0.1:4222`.

### Виртуальное окружение
Скрипты ищут виртуальное окружение в следующем порядке:
1. `qik-test-qa-server\ai\.venv\`
2. `.venv\` (корневое)

Рекомендуется создать виртуальное окружение в `qik-test-qa-server\ai\`:
```cmd
cd qik-test-qa-server\ai
python -m venv .venv
.venv\Scripts\activate.bat
pip install -r requirements.txt
```

---

## 🐛 Решение проблем

### Docker не найден
- Установите Docker Desktop
- Убедитесь, что Docker Desktop запущен
- Проверьте, что Docker добавлен в PATH

### NATS не запускается
- Проверьте, что Docker Desktop запущен и работает
- Убедитесь, что порты 4222 и 8222 свободны
- Проверьте файл `qik-test-qa-server\deploy\compose.proxy.yml`

### Python модули не найдены (ModuleNotFoundError)
- **РЕШЕНИЕ:** Запустите `setup_env.cmd` для установки всех зависимостей
- Или вручную:
  ```cmd
  cd qik-test-qa-server\ai
  .venv\Scripts\activate.bat
  pip install -r requirements.txt
  ```
- Проверьте, что виртуальное окружение активировано
- Проверьте PYTHONPATH

### Worker не подключается к NATS
- Убедитесь, что NATS контейнер запущен: `docker ps | findstr nats`
- Проверьте, что порт 4222 прослушивается: `netstat -an | findstr :4222`
- Проверьте переменную NATS_URL

---

## 📝 Дополнительная информация

Подробную документацию по архитектуре и использованию системы см. в:
- `README.md` - общая документация проекта
- `qik-test-qa-server\ai\README.md` - документация AI подпроекта
- `qik-test-qa-server\ai\docs\` - подробная документация

